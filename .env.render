# Render Environment Variables Template
# Copy these to your Render dashboard

# MongoDB
MONGODB_URI=mongodb+srv://techzealous100_db_user:2MFSX1MwrqJEwAbg@support-ticketing.6sj5h0j.mongodb.net/?retryWrites=true&w=majority&appName=support-ticketing

# Server Configuration
PORT=10000
NODE_ENV=production

# Frontend URL (update with your GitHub Pages URL)
FRONTEND_URL=http://killiansarsah.me/SupportDesk/

# Google OAuth (if using)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-for-production

# Email Configuration (if using)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password